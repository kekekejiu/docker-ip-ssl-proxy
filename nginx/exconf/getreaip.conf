# 已弃用：请改用 /etc/nginx/exconf/realip.conf
#
# 旧版本内容为 set_real_ip_from 0.0.0.0/0，会信任任意来源提交的
# X-Forwarded-For，使客户端可任意伪造自身 IP，导致 human-gate 风控、
# 频率限制与封禁全部失效。因此不再提供该配置。
#
# 保留本文件仅为兼容历史 include，不包含任何 real_ip 指令。
# 真实 IP 还原已在 nginx.conf 的 http{} 中通过 realip.conf 全局生效。
