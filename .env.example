# ZeroSSL API access key（https://app.zerossl.com/developer）
ZEROSSL_API_KEY=

# 留空则启动时自动探测本机公网 IPv4。多 IP 机器建议显式填写
CERT_IP=

CERT_VALIDITY_DAYS=90
RENEW_BEFORE_DAYS=30
CHECK_INTERVAL_HOURS=12

# 设为 1 强制重新签发，完成后改回 0
FORCE_ISSUE=0

# ============ human-gate 人机验证闸门 / 访客上报 ============
# 中心 ingest 地址（部署了 human-gate 分析中心的机器）
GATE_REPORT_URL=https://中心IP:9443/__gate/ingest
# 与中心 GATE_INGEST_TOKEN 一致的强密钥
GATE_REPORT_TOKEN=改成与中心一致的密钥
# 中心用自签/IP 证书时置 1，跳过证书校验（防伪造仍由 token 保证）
GATE_REPORT_INSECURE=1
# 通行证有效期（小时）
GATE_PASS_TTL_HOURS=12
